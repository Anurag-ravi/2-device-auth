<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
  </head>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/static/device-uuid.js" type="text/javascript"></script>
  <body>
    <section class="h-screen">
        <div class="px-6 h-full text-gray-800">
          <div
            class="flex xl:justify-center lg:justify-between justify-center items-center flex-wrap h-full g-6"
          >
            <div
              class="grow-0 shrink-1 md:shrink-0 basis-auto xl:w-6/12 lg:w-6/12 md:w-9/12 mb-12 md:mb-0"
            >
              <img
                src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/draw2.webp"
                class="w-full"
                alt="Sample image"
              />
            </div>
            <div class="xl:ml-20 xl:w-5/12 lg:w-5/12 md:w-8/12 mb-12 md:mb-0 rounded-lg border border-sky-500 p-4">
                <h1 class="text-3xl font-bold mb-4">Logined Devices</h1>
                <div class="grid-cols-2">

                    <% devices.forEach(function(device) { %>
                        <div class=" shadow-lg rounded-lg p-3 h-1/2">
                            <h2><%= device.browser %></h2>
                            <h3><%= device.isAndroid %></h3>
                            <h4><%= device.isDesktop %></h4>
                            <h5><%= device.isWindows %></h5>
                            <h6><%= device.isLinux %></h6>
                            <h6><%= device.isMac %></h6>
                            <h6><%= device.isiphone %></h6>
                            <h5><%= device.os %></h5>
                        </div>
                        <% }); %>
                </div>
            </div>
          </div>
        </div>
      </section>
    <script>
        /* const form = document.querySelector('form')
        const username = document.querySelector('#username')
        const password = document.querySelector('#password')
        const display = document.querySelector('.error')
        var uuid = new DeviceUUID().get();
        var du = new DeviceUUID().parse();
        form.addEventListener('submit', async (e) => {
          e.preventDefault()
          display.textContent = ''
          try {
            const res = await fetch('/api/login', {
              method: 'POST',
              body: JSON.stringify({ 
                username: username.value, 
                password: password.value, 
                uuid: uuid,
                browser: du.browser,
                isAndroid: du.isAndroid,
                isDesktop: du.isDesktop,
                os:du.os
            }),
              headers: { 'Content-Type': 'application/json' }
              })
            const data = await res.json()
            console.log(data)
            if (res.status === 400 || res.status === 401) {
              return display.textContent = `${data.error ? data.error : ''}`
            }
            if (res.status === 402) {
                location.assign('/logout')
            }
            // location.assign('/main')
          } catch (err) {
              console.log(err.message)
            }
  
          }) */
        </script>
      </body>
      </html>